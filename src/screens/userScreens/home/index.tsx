import React from 'react';
import { StyleSheet, Text, View } from 'react-native';

type HomeProps = {}; // Define your prop types here if needed

const Home: React.FC<HomeProps> = () => {
    return (
        <View>
            <Text>Home</Text>
        </View>
    );
};

export default Home;

const styles = StyleSheet.create({});
